

<!-- =======================
     Floating Buttons
======================= -->
<div class="floating-col" id="floatingCol">
  <!-- Lucky Wheel Item -->
  <div class="float-item" id="wheelItem" title="লাকি হুইল ঘোরাও">
    <div class="fi-icon" id="openWheel" aria-hidden="true">🎡</div>
    <div class="fi-close" data-target="wheelItem">&times;</div>
  </div>

  <!-- Facebook Item -->
  <div class="float-item" id="fbItem" title="ফেসবুকে যাও">
    <div class="fi-icon" id="openFB" aria-hidden="true">f</div>
    <div class="fi-label">ফেসবুক</div>
    <div class="fi-close" data-target="fbItem">&times;</div>
  </div>
</div>

<script>
  const fbc = document.getElementById("fbItem");
  const msgmodel = document.getElementById("msgcontainer");
  // Close floating items
  document.querySelectorAll('.fi-close').forEach(btn => {
    btn.addEventListener('click', () => {
      const targetId = btn.getAttribute('data-target');
      const el = document.getElementById(targetId);
      if (el) el.remove();
    });
  });
  
  
  const msgvv = document.createElement("div");
  Object.assign(msgvv.style, {
    display: "none",
    position: "fixed" ,
    zIndex: "1300",
    left: "0",
    top: "0",
    width: "100%",
    height: "100%",
    overflowY: "auto",
    backgroundColor: "rgba(0,0,0,0.9)"
  });
  
  fbc.addEventListener("click", () => {
    console.log("float")
    msgvv.style.display = "block";
    document.body.appendChild(msgvv);
  });
  
</script>
